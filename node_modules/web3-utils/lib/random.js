"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.randomHex = exports.randomBytes = void 0;
const crypto_1 = require("crypto");
const randomBytes = (byteSize) => {
    const randomValues = typeof window !== 'undefined' && window.crypto && window.crypto.getRandomValues
        ? window.crypto.getRandomValues(new Uint8Array(byteSize))
        : (0, crypto_1.randomBytes)(byteSize);
    return Buffer.from(randomValues);
};
exports.randomBytes = randomBytes;
const randomHex = (byteSize) => `0x${(0, exports.randomBytes)(byteSize).toString('hex')}`;
exports.randomHex = randomHex;
//# sourceMappingURL=random.js.map